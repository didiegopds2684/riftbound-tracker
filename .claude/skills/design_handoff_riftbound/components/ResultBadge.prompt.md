Result pill in win/loss/draw semantics — Vitória (green), Derrota (red), Empate (grey).

```jsx
<ResultBadge result="win" />
```

Accepts `'win' | 'loss' | 'draw'`. For arbitrary labels/colors use `Badge`.
